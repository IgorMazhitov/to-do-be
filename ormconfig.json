{
    "type": "postgres",
    "host": "localhost",
    "port": "${DB_PORT}",
    "username": "${DB_USERNAME}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_DATABASE}",
    "synchronize": true,
    "logging": true,
    "entities": ["src/**/*.entity{.ts,.js}"]
  }
  